import { useEffect,useState } from "react"; import AdminManagementShell from "./AdminManagementShell"; import api from "../../services/api";
export default function Payments(){const [rows,setRows]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=async()=>{try{setLoading(true);const r=await api.get("/admin/payments");setRows(r.data.payments||[])}catch(e){setError(e.response?.data?.message||"Unable to load payments.")}finally{setLoading(false)}};useEffect(()=>{load()},[]);return <AdminManagementShell title="Payments" description="Monitor Razorpay CV package transactions and payment status." onRefresh={load}>{error&&<div className="mb-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>{["Recruiter","Package","Amount","Credits","Status","Razorpay Order","Date"].map(x=><th key={x} className="px-5 py-4">{x}</th>)}</tr></thead><tbody className="divide-y">{loading?<tr><td colSpan="7" className="p-10 text-center">Loading...</td></tr>:rows.map(p=><tr key={p._id}><td className="px-5 py-4"><b>{p.recruiter?.name||"—"}</b><div className="text-xs text-slate-500">{p.recruiter?.email||""}</div></td><td className="px-5 py-4">{p.package?.name||"—"}</td><td className="px-5 py-4">₹{p.amount}</td><td className="px-5 py-4">{p.credits}</td><td className="px-5 py-4 capitalize">{p.status}</td><td className="px-5 py-4 font-mono text-xs">{p.razorpayOrderId||"—"}</td><td className="px-5 py-4">{new Date(p.createdAt).toLocaleString("en-IN")}</td></tr>)}</tbody></table></div></AdminManagementShell>}
